fixtures:
  repositories:
    stdlib:
      repo: "https://github.com/Adaptavist/puppet-stdlib"
    mysqlconfig:
      repo: "https://github.com/Adaptavist/puppet-mysqlconfig.git"
    mysql:
      repo: "https://github.com/Adaptavist/puppet-mysql"
  symlinks:
    database: "#{source_dir}"
